<?xml version="1.0" encoding="utf-8" ?>
<Shell xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
       xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
       xmlns:local="clr-namespace:mem1"
       x:Class="mem1.AppShell">

    <TabBar>
        <Tab Title="Главная">
            <ShellContent
                ContentTemplate="{DataTemplate local:MainPage}"
                Route="MainPage" />
        </Tab>

        <Tab Title="Коллекция">
            <ShellContent
                ContentTemplate="{DataTemplate local:SavedMemesPage}"
                Route="SavedMemesPage" />
        </Tab>

        <Tab Title="Редактор">
            <ShellContent
                ContentTemplate="{DataTemplate local:MemeEditorPage}"
                Route="MemeEditorPage" />
        </Tab>

        <Tab Title="Созданные">
            <ShellContent
                ContentTemplate="{DataTemplate local:CreatedMemesPage}"
                Route="CreatedMemesPage" />
        </Tab>
    </TabBar>

</Shell>